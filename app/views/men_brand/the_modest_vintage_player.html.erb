<%= render "templates/mobile_nav/navigation_mens" %>
<%= render "templates/mobile_nav/mobile_navigation_sliding_div_mens" %>
<%= render "templates/mens/navigation/navigation" %>
<div class="the_modest_vintage_player"></div>
<div class="desktop-categories"><h1>The Modest Vintage Player</h1></div>
<div class="container mobile_container">
  <div class="row">
    <h1 class="your-items mob-ul">The Modest Vintage Player</h1>
    <% @the_modest_vintage_player.each do |the_modest_vintage_player_items| %>
      <div class="box col-md-4 col-sm-6 col-xs-12">
        <div class="image_div">
          <% if the_modest_vintage_player_items.class == FashionItem %>
            <a href="/item/<%= the_modest_vintage_player_items.id %>">
              <%= image_tag the_modest_vintage_player_items.item_image.url(:medium) %>
              <div class="description_content">
                <% if the_modest_vintage_player_items.sale == true %>
                  <table>
                    <tr class="specials_table">
                    <td><strike><em>$<%= zeros(the_modest_vintage_player_items.price) %></em></strike></td>
                    <td>$<%= zeros(the_modest_vintage_player_items.sale_price) %></td>
                    </tr>
                  </table>
                <% else %>
                  <table>
                    <tr class="normal_table">
                    <td>$<%= zeros(the_modest_vintage_player_items.price) %></td>
                    </tr>
                  </table>
                <% end %>
                <p><%= the_modest_vintage_player_items.short_description %></p>
                <p><%= word_branding(the_modest_vintage_player_items.brand) %></p>
              </div>
            </a>
          <% elsif the_modest_vintage_player_items.class == ShopTheLookImage %>
            <a href="/shop_the_look_images/<%= the_modest_vintage_player_items.id %>">
              <%= image_tag the_modest_vintage_player_items.shop_look_image.url(:medium) %>
              <div class="description_content">
                <table>
                  <tr class="shop_look_table">
                  </tr>
                </table>
                <div class="shop-the-look-description">
                  <h2>Shop The Look</h2>
                  <p><%= word_branding(the_modest_vintage_player_items.shop_the_look_items.first.brand) %></p>
                </div>
              </div>
            </a>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<%= render 'templates/footer/footer' %>