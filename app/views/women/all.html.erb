<% if current_admin %>
  <%= render "templates/admin_navigation" %>
<% else %>
  <%= render "templates/navigation" %>
<% end %>
<div class="menu-side">
  <div class="margin_menu_side"></div>
  <%= render "templates/side_navigation_womens" %>
</div>
<div class="container">
  <div class="row" style="margin-left: 0px;">
    <div class="item_navigation">
      <%= render "templates/item_navigation_womens_subcat" %>
    </div>
    <h1 class="your-items">All Womens</h1>
    <% @womens.each do |all| %>
      <div class="box col-md-4 col-sm-6 col-xs-12">
        <div class="image_div">
          <a href="/item/<%= all.id %>">
            <%= image_tag all.item_image.url(:medium) %>
          <div class="description_content">
            <% if all.sale == true %>
            <table>
              <tr class="specials_table">
                <td><strike><em>$<%= zeros(all.price) %></em></strike></td>
                <td>$<%= zeros(all.sale_price) %></td>
              </tr>
            </table>
            <% else %>
            <table>
              <tr class="normal_table">
                <td>$<%= zeros(all.price) %></td>
              </tr>
            </table>
            <% end %>
            <p><%= all.short_description %></p>
            <p><%= word_branding(all.brand) %></p>
          </div>
          </a>
        </div>
      </div>
    <% end %>
  </div>
</div>