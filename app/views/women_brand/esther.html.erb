<%= render "templates/mobile_nav/navigation_womens" %>
<%= render "templates/mobile_nav/mobile_navigation_sliding_div_women" %>
<%= render "templates/womens/navigation/navigation" %>
<div class="esther"></div>
<div class="desktop-categories"><h1>Esther Boutique</h1></div>
<div class="container mobile_container">
  <div class="row">
    <h1 class="your-items mob-ul">Esther Boutique</h1>
    <% @esther.each do |esther_items| %>
      <div class="box col-md-4 col-sm-6 col-xs-12">
        <div class="image_div">
          <% if esther_items.class == FashionItem %>
            <a href="/item/<%= esther_items.id %>">
              <%= image_tag esther_items.item_image.url(:medium) %>
              <div class="description_content">
                <% if esther_items.sale == true %>
                  <table>
                    <tr class="specials_table">
                    <td><strike><em>$<%= zeros(esther_items.price) %></em></strike></td>
                    <td>$<%= zeros(esther_items.sale_price) %></td>
                    </tr>
                  </table>
                <% else %>
                  <table>
                    <tr class="normal_table">
                    <td>$<%= zeros(esther_items.price) %></td>
                    </tr>
                  </table>
                <% end %>
                <p><%= esther_items.short_description %></p>
                <p><%= word_branding(esther_items.brand) %></p>
              </div>
            </a>
          <% elsif esther_items.class == ShopTheLookImage %>
            <a href="/shop_the_look_images/<%= esther_items.id %>">
              <%= image_tag esther_items.shop_look_image.url(:medium) %>
              <div class="description_content">
                <table>
                  <tr class="shop_look_table">
                  </tr>
                </table>
                <div class="shop-the-look-description">
                  <h2>Shop The Look</h2>
                  <p><%= word_branding(esther_items.shop_the_look_items.first.brand) %></p>
                </div>
              </div>
            </a>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<%= render "templates/footer/footer_women" %>