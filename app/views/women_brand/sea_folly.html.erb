<%= render "templates/mobile_nav/navigation_womens" %>
<%= render "templates/mobile_nav/mobile_navigation_sliding_div_women" %>
<%= render "templates/womens/navigation/navigation" %>
<div class="sea_folly_womens"></div>
<div class="desktop-categories"><h1>Sea Folly</h1></div>
<div class="container mobile_container">
  <div class="row">
    <h1 class="your-items mob-ul">Sea Folly</h1>
    <% @sea_folly.each do |sea_folly_items| %>
      <div class="box col-md-4 col-sm-6 col-xs-12">
        <div class="image_div">
          <% if sea_folly_items.class == FashionItem %>
            <a href="/item/<%= sea_folly_items.id %>">
              <%= image_tag sea_folly_items.item_image.url(:medium) %>
              <div class="description_content">
                <% if sea_folly_items.sale == true %>
                  <table>
                    <tr class="specials_table">
                    <td><strike><em>$<%= zeros(sea_folly_items.price) %></em></strike></td>
                    <td>$<%= zeros(sea_folly_items.sale_price) %></td>
                    </tr>
                  </table>
                <% else %>
                  <table>
                    <tr class="normal_table">
                    <td>$<%= zeros(sea_folly_items.price) %></td>
                    </tr>
                  </table>
                <% end %>
                <p><%= sea_folly_items.short_description %></p>
                <p><%= word_branding(sea_folly_items.brand) %></p>
              </div>
            </a>
          <% elsif sea_folly_items.class == ShopTheLookImage %>
            <a href="/shop_the_look_images/<%= sea_folly_items.id %>">
              <%= image_tag sea_folly_items.shop_look_image.url(:medium) %>
              <div class="description_content">
                <table>
                  <tr class="shop_look_table">
                  </tr>
                </table>
                <div class="shop-the-look-description">
                  <h2>Shop The Look</h2>
                  <p><%= word_branding(sea_folly_items.shop_the_look_items.first.brand) %></p>
                </div>
              </div>
            </a>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<%= render "templates/footer/footer_women" %>